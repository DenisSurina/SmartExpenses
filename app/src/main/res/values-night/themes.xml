<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SmartExpenses" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/red</item>  <!--  buttons -->
        <item name="colorPrimaryVariant">@color/red</item>
        <item name="colorOnPrimary">@color/red</item>  <!--  background -->
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/red</item>
        <item name="colorSecondaryVariant">@color/red</item>
        <item name="colorOnSecondary">@color/red</item> <!--  background -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/red</item> <!-- Card view background -->
        <!-- Customize your theme here. -->
        <!-- Customize your theme here. -->
        <item name="android:padding">0dp</item>
        <item name="android:radius">1000px</item>
        <item name="cornerRadius">0dp</item>

        <item name="background_primary">@color/red</item>
        <item name="background_secondary">@color/red</item>
        <item name="stroke_color">@color/red</item>

        <item name="button_primary">@color/red</item>
        <item name="button_font">@color/red</item>
        <item name="text_primary">@color/red</item>

    </style>

    <style name="ButtonSmartExpense" parent="Widget.AppCompat.Button">
        <item name="android:backgroundTint">@color/red</item>
        <!-- Define other properties as needed -->
    </style>

    <style name="BottomNavTextSize">
        <item name="android:textSize">50sp</item> <!-- Adjust the text size as needed -->
    </style>

</resources>